#include <main.h>
#include <stdio.h>

/**
 ** main - check the code
 **
 ** Return: Always 0.
 **/

void print_alphabet(void);

int main(void)
{	char letter;
	for (letter = 'a'; c <='z'; ++letter)
	printf ("%c", c);
	return (0);
}
